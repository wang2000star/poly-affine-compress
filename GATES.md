# 电路文件使用的门

本项目的电路文件（.txt）只使用以下三种门：

| 门 | 运算符 | 示例 |
|----|--------|------|
| NOT | `!` | `n1 = !i_0` |
| AND | `*` | `n2 = n1 * i_1` |
| XOR | `+` | `n3 = n2 + n1` |

**没有** NOR、OR、NAND、XNOR、AND-OR-INVERT 等任何复合门。所有逻辑都是仅用 NOT、AND、XOR 三种基本门表达的。

常量：`0` 或 `1`。
别名：`m_0 = i_0`（直接传递输入）。
