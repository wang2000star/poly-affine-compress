# Raw ANF for circuit (n=8, k=256 outputs)
# osel10
count7
count0 * count7
count1 * count7
count0 * count1 * count7
count2 * count7
count0 * count2 * count7
count1 * count2 * count7
count0 * count1 * count2 * count7
count3 * count7
count0 * count3 * count7
count1 * count3 * count7
count0 * count1 * count3 * count7
count2 * count3 * count7
count0 * count2 * count3 * count7
count1 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count4 * count7
count0 * count4 * count7
count1 * count4 * count7
count0 * count1 * count4 * count7
count2 * count4 * count7
count0 * count2 * count4 * count7
count1 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count3 * count4 * count7
count0 * count3 * count4 * count7
count1 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count2 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count5 * count7
count0 * count5 * count7
count1 * count5 * count7
count0 * count1 * count5 * count7
count2 * count5 * count7
count0 * count2 * count5 * count7
count1 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count3 * count5 * count7
count0 * count3 * count5 * count7
count1 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count2 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count4 * count5 * count7
count0 * count4 * count5 * count7
count1 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count2 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count3 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count6 * count7
count0 * count6 * count7
count1 * count6 * count7
count0 * count1 * count6 * count7
count2 * count6 * count7
count0 * count2 * count6 * count7
count1 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count3 * count6 * count7
count0 * count3 * count6 * count7
count1 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count2 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count4 * count6 * count7
count0 * count4 * count6 * count7
count1 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count2 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count3 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count5 * count6 * count7
count0 * count5 * count6 * count7
count1 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count2 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count3 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count4 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel10 total: 128 terms
# osel11
count0 * count7
count0 * count1 * count7
count0 * count2 * count7
count0 * count1 * count2 * count7
count0 * count3 * count7
count0 * count1 * count3 * count7
count0 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count0 * count4 * count7
count0 * count1 * count4 * count7
count0 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count0 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count5 * count7
count0 * count1 * count5 * count7
count0 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count0 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count6 * count7
count0 * count1 * count6 * count7
count0 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count0 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel11 total: 64 terms
# osel12
count1 * count7
count0 * count1 * count7
count1 * count2 * count7
count0 * count1 * count2 * count7
count1 * count3 * count7
count0 * count1 * count3 * count7
count1 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count1 * count4 * count7
count0 * count1 * count4 * count7
count1 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count1 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count1 * count5 * count7
count0 * count1 * count5 * count7
count1 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count1 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count1 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count6 * count7
count0 * count1 * count6 * count7
count1 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count1 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count1 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel12 total: 64 terms
# osel13
count0 * count1 * count7
count0 * count1 * count2 * count7
count0 * count1 * count3 * count7
count0 * count1 * count2 * count3 * count7
count0 * count1 * count4 * count7
count0 * count1 * count2 * count4 * count7
count0 * count1 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count1 * count5 * count7
count0 * count1 * count2 * count5 * count7
count0 * count1 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count1 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count6 * count7
count0 * count1 * count2 * count6 * count7
count0 * count1 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count1 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel13 total: 32 terms
# osel14
count2 * count7
count0 * count2 * count7
count1 * count2 * count7
count0 * count1 * count2 * count7
count2 * count3 * count7
count0 * count2 * count3 * count7
count1 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count2 * count4 * count7
count0 * count2 * count4 * count7
count1 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count2 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count2 * count5 * count7
count0 * count2 * count5 * count7
count1 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count2 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count2 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count2 * count6 * count7
count0 * count2 * count6 * count7
count1 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count2 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count2 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count2 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel14 total: 64 terms
# osel15
count0 * count2 * count7
count0 * count1 * count2 * count7
count0 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count0 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count0 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count0 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count0 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel15 total: 32 terms
# osel16
count1 * count2 * count7
count0 * count1 * count2 * count7
count1 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count1 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count1 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel16 total: 32 terms
# osel17
count0 * count1 * count2 * count7
count0 * count1 * count2 * count3 * count7
count0 * count1 * count2 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel17 total: 16 terms
# osel18
count3 * count7
count0 * count3 * count7
count1 * count3 * count7
count0 * count1 * count3 * count7
count2 * count3 * count7
count0 * count2 * count3 * count7
count1 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count3 * count4 * count7
count0 * count3 * count4 * count7
count1 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count2 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count3 * count5 * count7
count0 * count3 * count5 * count7
count1 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count2 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count3 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count3 * count6 * count7
count0 * count3 * count6 * count7
count1 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count2 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count3 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count3 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel18 total: 64 terms
# osel19
count0 * count3 * count7
count0 * count1 * count3 * count7
count0 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count0 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel19 total: 32 terms
# osel110
count1 * count3 * count7
count0 * count1 * count3 * count7
count1 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count1 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count1 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel110 total: 32 terms
# osel111
count0 * count1 * count3 * count7
count0 * count1 * count2 * count3 * count7
count0 * count1 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count1 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel111 total: 16 terms
# osel112
count2 * count3 * count7
count0 * count2 * count3 * count7
count1 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count2 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count2 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count2 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel112 total: 32 terms
# osel113
count0 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count0 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel113 total: 16 terms
# osel114
count1 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel114 total: 16 terms
# osel115
count0 * count1 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel115 total: 8 terms
# osel116
count4 * count7
count0 * count4 * count7
count1 * count4 * count7
count0 * count1 * count4 * count7
count2 * count4 * count7
count0 * count2 * count4 * count7
count1 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count3 * count4 * count7
count0 * count3 * count4 * count7
count1 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count2 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count4 * count5 * count7
count0 * count4 * count5 * count7
count1 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count2 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count3 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count4 * count6 * count7
count0 * count4 * count6 * count7
count1 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count2 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count3 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count4 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel116 total: 64 terms
# osel117
count0 * count4 * count7
count0 * count1 * count4 * count7
count0 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count0 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel117 total: 32 terms
# osel118
count1 * count4 * count7
count0 * count1 * count4 * count7
count1 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count1 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count1 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel118 total: 32 terms
# osel119
count0 * count1 * count4 * count7
count0 * count1 * count2 * count4 * count7
count0 * count1 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count1 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel119 total: 16 terms
# osel120
count2 * count4 * count7
count0 * count2 * count4 * count7
count1 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count2 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count2 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count2 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel120 total: 32 terms
# osel121
count0 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count0 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel121 total: 16 terms
# osel122
count1 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel122 total: 16 terms
# osel123
count0 * count1 * count2 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel123 total: 8 terms
# osel124
count3 * count4 * count7
count0 * count3 * count4 * count7
count1 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count2 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count3 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count3 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel124 total: 32 terms
# osel125
count0 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel125 total: 16 terms
# osel126
count1 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel126 total: 16 terms
# osel127
count0 * count1 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel127 total: 8 terms
# osel128
count2 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel128 total: 16 terms
# osel129
count0 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel129 total: 8 terms
# osel130
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel130 total: 8 terms
# osel131
count0 * count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel131 total: 4 terms
# osel132
count5 * count7
count0 * count5 * count7
count1 * count5 * count7
count0 * count1 * count5 * count7
count2 * count5 * count7
count0 * count2 * count5 * count7
count1 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count3 * count5 * count7
count0 * count3 * count5 * count7
count1 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count2 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count4 * count5 * count7
count0 * count4 * count5 * count7
count1 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count2 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count3 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count5 * count6 * count7
count0 * count5 * count6 * count7
count1 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count2 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count3 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count4 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel132 total: 64 terms
# osel133
count0 * count5 * count7
count0 * count1 * count5 * count7
count0 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count0 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel133 total: 32 terms
# osel134
count1 * count5 * count7
count0 * count1 * count5 * count7
count1 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count1 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count1 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel134 total: 32 terms
# osel135
count0 * count1 * count5 * count7
count0 * count1 * count2 * count5 * count7
count0 * count1 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count1 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel135 total: 16 terms
# osel136
count2 * count5 * count7
count0 * count2 * count5 * count7
count1 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count2 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count2 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count2 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel136 total: 32 terms
# osel137
count0 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count0 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel137 total: 16 terms
# osel138
count1 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel138 total: 16 terms
# osel139
count0 * count1 * count2 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel139 total: 8 terms
# osel140
count3 * count5 * count7
count0 * count3 * count5 * count7
count1 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count2 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count3 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count3 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel140 total: 32 terms
# osel141
count0 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel141 total: 16 terms
# osel142
count1 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel142 total: 16 terms
# osel143
count0 * count1 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel143 total: 8 terms
# osel144
count2 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel144 total: 16 terms
# osel145
count0 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel145 total: 8 terms
# osel146
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel146 total: 8 terms
# osel147
count0 * count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel147 total: 4 terms
# osel148
count4 * count5 * count7
count0 * count4 * count5 * count7
count1 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count2 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count3 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count4 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel148 total: 32 terms
# osel149
count0 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel149 total: 16 terms
# osel150
count1 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel150 total: 16 terms
# osel151
count0 * count1 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel151 total: 8 terms
# osel152
count2 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel152 total: 16 terms
# osel153
count0 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel153 total: 8 terms
# osel154
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel154 total: 8 terms
# osel155
count0 * count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel155 total: 4 terms
# osel156
count3 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel156 total: 16 terms
# osel157
count0 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel157 total: 8 terms
# osel158
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel158 total: 8 terms
# osel159
count0 * count1 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel159 total: 4 terms
# osel160
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel160 total: 8 terms
# osel161
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel161 total: 4 terms
# osel162
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel162 total: 4 terms
# osel163
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel163 total: 2 terms
# osel164
count6 * count7
count0 * count6 * count7
count1 * count6 * count7
count0 * count1 * count6 * count7
count2 * count6 * count7
count0 * count2 * count6 * count7
count1 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count3 * count6 * count7
count0 * count3 * count6 * count7
count1 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count2 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count4 * count6 * count7
count0 * count4 * count6 * count7
count1 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count2 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count3 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count5 * count6 * count7
count0 * count5 * count6 * count7
count1 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count2 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count3 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count4 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel164 total: 64 terms
# osel165
count0 * count6 * count7
count0 * count1 * count6 * count7
count0 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count0 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel165 total: 32 terms
# osel166
count1 * count6 * count7
count0 * count1 * count6 * count7
count1 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count1 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count1 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel166 total: 32 terms
# osel167
count0 * count1 * count6 * count7
count0 * count1 * count2 * count6 * count7
count0 * count1 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count1 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel167 total: 16 terms
# osel168
count2 * count6 * count7
count0 * count2 * count6 * count7
count1 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count2 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count2 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count2 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel168 total: 32 terms
# osel169
count0 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count0 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel169 total: 16 terms
# osel170
count1 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel170 total: 16 terms
# osel171
count0 * count1 * count2 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel171 total: 8 terms
# osel172
count3 * count6 * count7
count0 * count3 * count6 * count7
count1 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count2 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count3 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count3 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel172 total: 32 terms
# osel173
count0 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel173 total: 16 terms
# osel174
count1 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel174 total: 16 terms
# osel175
count0 * count1 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel175 total: 8 terms
# osel176
count2 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel176 total: 16 terms
# osel177
count0 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel177 total: 8 terms
# osel178
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel178 total: 8 terms
# osel179
count0 * count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel179 total: 4 terms
# osel180
count4 * count6 * count7
count0 * count4 * count6 * count7
count1 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count2 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count3 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count4 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel180 total: 32 terms
# osel181
count0 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel181 total: 16 terms
# osel182
count1 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel182 total: 16 terms
# osel183
count0 * count1 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel183 total: 8 terms
# osel184
count2 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel184 total: 16 terms
# osel185
count0 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel185 total: 8 terms
# osel186
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel186 total: 8 terms
# osel187
count0 * count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel187 total: 4 terms
# osel188
count3 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel188 total: 16 terms
# osel189
count0 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel189 total: 8 terms
# osel190
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel190 total: 8 terms
# osel191
count0 * count1 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel191 total: 4 terms
# osel192
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel192 total: 8 terms
# osel193
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel193 total: 4 terms
# osel194
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel194 total: 4 terms
# osel195
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel195 total: 2 terms
# osel196
count5 * count6 * count7
count0 * count5 * count6 * count7
count1 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count2 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count3 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count4 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel196 total: 32 terms
# osel197
count0 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel197 total: 16 terms
# osel198
count1 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel198 total: 16 terms
# osel199
count0 * count1 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel199 total: 8 terms
# osel1100
count2 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1100 total: 16 terms
# osel1101
count0 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1101 total: 8 terms
# osel1102
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1102 total: 8 terms
# osel1103
count0 * count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1103 total: 4 terms
# osel1104
count3 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1104 total: 16 terms
# osel1105
count0 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1105 total: 8 terms
# osel1106
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1106 total: 8 terms
# osel1107
count0 * count1 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1107 total: 4 terms
# osel1108
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1108 total: 8 terms
# osel1109
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1109 total: 4 terms
# osel1110
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1110 total: 4 terms
# osel1111
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1111 total: 2 terms
# osel1112
count4 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1112 total: 16 terms
# osel1113
count0 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1113 total: 8 terms
# osel1114
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1114 total: 8 terms
# osel1115
count0 * count1 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1115 total: 4 terms
# osel1116
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1116 total: 8 terms
# osel1117
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1117 total: 4 terms
# osel1118
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1118 total: 4 terms
# osel1119
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1119 total: 2 terms
# osel1120
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1120 total: 8 terms
# osel1121
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1121 total: 4 terms
# osel1122
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1122 total: 4 terms
# osel1123
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1123 total: 2 terms
# osel1124
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1124 total: 4 terms
# osel1125
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1125 total: 2 terms
# osel1126
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1126 total: 2 terms
# osel1127
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel1127 total: 1 terms
# osel20
1
count0
count1
count0 * count1
count2
count0 * count2
count1 * count2
count0 * count1 * count2
count3
count0 * count3
count1 * count3
count0 * count1 * count3
count2 * count3
count0 * count2 * count3
count1 * count2 * count3
count0 * count1 * count2 * count3
count4
count0 * count4
count1 * count4
count0 * count1 * count4
count2 * count4
count0 * count2 * count4
count1 * count2 * count4
count0 * count1 * count2 * count4
count3 * count4
count0 * count3 * count4
count1 * count3 * count4
count0 * count1 * count3 * count4
count2 * count3 * count4
count0 * count2 * count3 * count4
count1 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count5
count0 * count5
count1 * count5
count0 * count1 * count5
count2 * count5
count0 * count2 * count5
count1 * count2 * count5
count0 * count1 * count2 * count5
count3 * count5
count0 * count3 * count5
count1 * count3 * count5
count0 * count1 * count3 * count5
count2 * count3 * count5
count0 * count2 * count3 * count5
count1 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count4 * count5
count0 * count4 * count5
count1 * count4 * count5
count0 * count1 * count4 * count5
count2 * count4 * count5
count0 * count2 * count4 * count5
count1 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count3 * count4 * count5
count0 * count3 * count4 * count5
count1 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count2 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count6
count0 * count6
count1 * count6
count0 * count1 * count6
count2 * count6
count0 * count2 * count6
count1 * count2 * count6
count0 * count1 * count2 * count6
count3 * count6
count0 * count3 * count6
count1 * count3 * count6
count0 * count1 * count3 * count6
count2 * count3 * count6
count0 * count2 * count3 * count6
count1 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count4 * count6
count0 * count4 * count6
count1 * count4 * count6
count0 * count1 * count4 * count6
count2 * count4 * count6
count0 * count2 * count4 * count6
count1 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count3 * count4 * count6
count0 * count3 * count4 * count6
count1 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count2 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count5 * count6
count0 * count5 * count6
count1 * count5 * count6
count0 * count1 * count5 * count6
count2 * count5 * count6
count0 * count2 * count5 * count6
count1 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count3 * count5 * count6
count0 * count3 * count5 * count6
count1 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count2 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count4 * count5 * count6
count0 * count4 * count5 * count6
count1 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count2 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count3 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count7
count0 * count7
count1 * count7
count0 * count1 * count7
count2 * count7
count0 * count2 * count7
count1 * count2 * count7
count0 * count1 * count2 * count7
count3 * count7
count0 * count3 * count7
count1 * count3 * count7
count0 * count1 * count3 * count7
count2 * count3 * count7
count0 * count2 * count3 * count7
count1 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count4 * count7
count0 * count4 * count7
count1 * count4 * count7
count0 * count1 * count4 * count7
count2 * count4 * count7
count0 * count2 * count4 * count7
count1 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count3 * count4 * count7
count0 * count3 * count4 * count7
count1 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count2 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count5 * count7
count0 * count5 * count7
count1 * count5 * count7
count0 * count1 * count5 * count7
count2 * count5 * count7
count0 * count2 * count5 * count7
count1 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count3 * count5 * count7
count0 * count3 * count5 * count7
count1 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count2 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count4 * count5 * count7
count0 * count4 * count5 * count7
count1 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count2 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count3 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count6 * count7
count0 * count6 * count7
count1 * count6 * count7
count0 * count1 * count6 * count7
count2 * count6 * count7
count0 * count2 * count6 * count7
count1 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count3 * count6 * count7
count0 * count3 * count6 * count7
count1 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count2 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count4 * count6 * count7
count0 * count4 * count6 * count7
count1 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count2 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count3 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count5 * count6 * count7
count0 * count5 * count6 * count7
count1 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count2 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count3 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count4 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel20 total: 256 terms
# osel21
count0
count0 * count1
count0 * count2
count0 * count1 * count2
count0 * count3
count0 * count1 * count3
count0 * count2 * count3
count0 * count1 * count2 * count3
count0 * count4
count0 * count1 * count4
count0 * count2 * count4
count0 * count1 * count2 * count4
count0 * count3 * count4
count0 * count1 * count3 * count4
count0 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count0 * count5
count0 * count1 * count5
count0 * count2 * count5
count0 * count1 * count2 * count5
count0 * count3 * count5
count0 * count1 * count3 * count5
count0 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count0 * count4 * count5
count0 * count1 * count4 * count5
count0 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count0 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count6
count0 * count1 * count6
count0 * count2 * count6
count0 * count1 * count2 * count6
count0 * count3 * count6
count0 * count1 * count3 * count6
count0 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count0 * count4 * count6
count0 * count1 * count4 * count6
count0 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count0 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count5 * count6
count0 * count1 * count5 * count6
count0 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count0 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count7
count0 * count1 * count7
count0 * count2 * count7
count0 * count1 * count2 * count7
count0 * count3 * count7
count0 * count1 * count3 * count7
count0 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count0 * count4 * count7
count0 * count1 * count4 * count7
count0 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count0 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count5 * count7
count0 * count1 * count5 * count7
count0 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count0 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count6 * count7
count0 * count1 * count6 * count7
count0 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count0 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel21 total: 128 terms
# osel22
count1
count0 * count1
count1 * count2
count0 * count1 * count2
count1 * count3
count0 * count1 * count3
count1 * count2 * count3
count0 * count1 * count2 * count3
count1 * count4
count0 * count1 * count4
count1 * count2 * count4
count0 * count1 * count2 * count4
count1 * count3 * count4
count0 * count1 * count3 * count4
count1 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count1 * count5
count0 * count1 * count5
count1 * count2 * count5
count0 * count1 * count2 * count5
count1 * count3 * count5
count0 * count1 * count3 * count5
count1 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count1 * count4 * count5
count0 * count1 * count4 * count5
count1 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count1 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count1 * count6
count0 * count1 * count6
count1 * count2 * count6
count0 * count1 * count2 * count6
count1 * count3 * count6
count0 * count1 * count3 * count6
count1 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count1 * count4 * count6
count0 * count1 * count4 * count6
count1 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count1 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count1 * count5 * count6
count0 * count1 * count5 * count6
count1 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count1 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count1 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count7
count0 * count1 * count7
count1 * count2 * count7
count0 * count1 * count2 * count7
count1 * count3 * count7
count0 * count1 * count3 * count7
count1 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count1 * count4 * count7
count0 * count1 * count4 * count7
count1 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count1 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count1 * count5 * count7
count0 * count1 * count5 * count7
count1 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count1 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count1 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count6 * count7
count0 * count1 * count6 * count7
count1 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count1 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count1 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel22 total: 128 terms
# osel23
count0 * count1
count0 * count1 * count2
count0 * count1 * count3
count0 * count1 * count2 * count3
count0 * count1 * count4
count0 * count1 * count2 * count4
count0 * count1 * count3 * count4
count0 * count1 * count2 * count3 * count4
count0 * count1 * count5
count0 * count1 * count2 * count5
count0 * count1 * count3 * count5
count0 * count1 * count2 * count3 * count5
count0 * count1 * count4 * count5
count0 * count1 * count2 * count4 * count5
count0 * count1 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count1 * count6
count0 * count1 * count2 * count6
count0 * count1 * count3 * count6
count0 * count1 * count2 * count3 * count6
count0 * count1 * count4 * count6
count0 * count1 * count2 * count4 * count6
count0 * count1 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count1 * count5 * count6
count0 * count1 * count2 * count5 * count6
count0 * count1 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count1 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count7
count0 * count1 * count2 * count7
count0 * count1 * count3 * count7
count0 * count1 * count2 * count3 * count7
count0 * count1 * count4 * count7
count0 * count1 * count2 * count4 * count7
count0 * count1 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count1 * count5 * count7
count0 * count1 * count2 * count5 * count7
count0 * count1 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count1 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count6 * count7
count0 * count1 * count2 * count6 * count7
count0 * count1 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count1 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel23 total: 64 terms
# osel24
count2
count0 * count2
count1 * count2
count0 * count1 * count2
count2 * count3
count0 * count2 * count3
count1 * count2 * count3
count0 * count1 * count2 * count3
count2 * count4
count0 * count2 * count4
count1 * count2 * count4
count0 * count1 * count2 * count4
count2 * count3 * count4
count0 * count2 * count3 * count4
count1 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count2 * count5
count0 * count2 * count5
count1 * count2 * count5
count0 * count1 * count2 * count5
count2 * count3 * count5
count0 * count2 * count3 * count5
count1 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count2 * count4 * count5
count0 * count2 * count4 * count5
count1 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count2 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count2 * count6
count0 * count2 * count6
count1 * count2 * count6
count0 * count1 * count2 * count6
count2 * count3 * count6
count0 * count2 * count3 * count6
count1 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count2 * count4 * count6
count0 * count2 * count4 * count6
count1 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count2 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count2 * count5 * count6
count0 * count2 * count5 * count6
count1 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count2 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count2 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count2 * count7
count0 * count2 * count7
count1 * count2 * count7
count0 * count1 * count2 * count7
count2 * count3 * count7
count0 * count2 * count3 * count7
count1 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count2 * count4 * count7
count0 * count2 * count4 * count7
count1 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count2 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count2 * count5 * count7
count0 * count2 * count5 * count7
count1 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count2 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count2 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count2 * count6 * count7
count0 * count2 * count6 * count7
count1 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count2 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count2 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count2 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel24 total: 128 terms
# osel25
count0 * count2
count0 * count1 * count2
count0 * count2 * count3
count0 * count1 * count2 * count3
count0 * count2 * count4
count0 * count1 * count2 * count4
count0 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count0 * count2 * count5
count0 * count1 * count2 * count5
count0 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count0 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count0 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count2 * count6
count0 * count1 * count2 * count6
count0 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count0 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count0 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count0 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count2 * count7
count0 * count1 * count2 * count7
count0 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count0 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count0 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count0 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count0 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel25 total: 64 terms
# osel26
count1 * count2
count0 * count1 * count2
count1 * count2 * count3
count0 * count1 * count2 * count3
count1 * count2 * count4
count0 * count1 * count2 * count4
count1 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count1 * count2 * count5
count0 * count1 * count2 * count5
count1 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count1 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count1 * count2 * count6
count0 * count1 * count2 * count6
count1 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count1 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count1 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count7
count0 * count1 * count2 * count7
count1 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count1 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count1 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel26 total: 64 terms
# osel27
count0 * count1 * count2
count0 * count1 * count2 * count3
count0 * count1 * count2 * count4
count0 * count1 * count2 * count3 * count4
count0 * count1 * count2 * count5
count0 * count1 * count2 * count3 * count5
count0 * count1 * count2 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count6
count0 * count1 * count2 * count3 * count6
count0 * count1 * count2 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count7
count0 * count1 * count2 * count3 * count7
count0 * count1 * count2 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel27 total: 32 terms
# osel28
count3
count0 * count3
count1 * count3
count0 * count1 * count3
count2 * count3
count0 * count2 * count3
count1 * count2 * count3
count0 * count1 * count2 * count3
count3 * count4
count0 * count3 * count4
count1 * count3 * count4
count0 * count1 * count3 * count4
count2 * count3 * count4
count0 * count2 * count3 * count4
count1 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count3 * count5
count0 * count3 * count5
count1 * count3 * count5
count0 * count1 * count3 * count5
count2 * count3 * count5
count0 * count2 * count3 * count5
count1 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count3 * count4 * count5
count0 * count3 * count4 * count5
count1 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count2 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count3 * count6
count0 * count3 * count6
count1 * count3 * count6
count0 * count1 * count3 * count6
count2 * count3 * count6
count0 * count2 * count3 * count6
count1 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count3 * count4 * count6
count0 * count3 * count4 * count6
count1 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count2 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count3 * count5 * count6
count0 * count3 * count5 * count6
count1 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count2 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count3 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count3 * count7
count0 * count3 * count7
count1 * count3 * count7
count0 * count1 * count3 * count7
count2 * count3 * count7
count0 * count2 * count3 * count7
count1 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count3 * count4 * count7
count0 * count3 * count4 * count7
count1 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count2 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count3 * count5 * count7
count0 * count3 * count5 * count7
count1 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count2 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count3 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count3 * count6 * count7
count0 * count3 * count6 * count7
count1 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count2 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count3 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count3 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel28 total: 128 terms
# osel29
count0 * count3
count0 * count1 * count3
count0 * count2 * count3
count0 * count1 * count2 * count3
count0 * count3 * count4
count0 * count1 * count3 * count4
count0 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count0 * count3 * count5
count0 * count1 * count3 * count5
count0 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count0 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count3 * count6
count0 * count1 * count3 * count6
count0 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count0 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count3 * count7
count0 * count1 * count3 * count7
count0 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count0 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel29 total: 64 terms
# osel210
count1 * count3
count0 * count1 * count3
count1 * count2 * count3
count0 * count1 * count2 * count3
count1 * count3 * count4
count0 * count1 * count3 * count4
count1 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count1 * count3 * count5
count0 * count1 * count3 * count5
count1 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count1 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count1 * count3 * count6
count0 * count1 * count3 * count6
count1 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count1 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count1 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count3 * count7
count0 * count1 * count3 * count7
count1 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count1 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count1 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel210 total: 64 terms
# osel211
count0 * count1 * count3
count0 * count1 * count2 * count3
count0 * count1 * count3 * count4
count0 * count1 * count2 * count3 * count4
count0 * count1 * count3 * count5
count0 * count1 * count2 * count3 * count5
count0 * count1 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count1 * count3 * count6
count0 * count1 * count2 * count3 * count6
count0 * count1 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count1 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count7
count0 * count1 * count2 * count3 * count7
count0 * count1 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count1 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel211 total: 32 terms
# osel212
count2 * count3
count0 * count2 * count3
count1 * count2 * count3
count0 * count1 * count2 * count3
count2 * count3 * count4
count0 * count2 * count3 * count4
count1 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count2 * count3 * count5
count0 * count2 * count3 * count5
count1 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count2 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count2 * count3 * count6
count0 * count2 * count3 * count6
count1 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count2 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count2 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count2 * count3 * count7
count0 * count2 * count3 * count7
count1 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count2 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count2 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count2 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel212 total: 64 terms
# osel213
count0 * count2 * count3
count0 * count1 * count2 * count3
count0 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count0 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count0 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count0 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count0 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel213 total: 32 terms
# osel214
count1 * count2 * count3
count0 * count1 * count2 * count3
count1 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count1 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel214 total: 32 terms
# osel215
count0 * count1 * count2 * count3
count0 * count1 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel215 total: 16 terms
# osel216
count4
count0 * count4
count1 * count4
count0 * count1 * count4
count2 * count4
count0 * count2 * count4
count1 * count2 * count4
count0 * count1 * count2 * count4
count3 * count4
count0 * count3 * count4
count1 * count3 * count4
count0 * count1 * count3 * count4
count2 * count3 * count4
count0 * count2 * count3 * count4
count1 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count4 * count5
count0 * count4 * count5
count1 * count4 * count5
count0 * count1 * count4 * count5
count2 * count4 * count5
count0 * count2 * count4 * count5
count1 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count3 * count4 * count5
count0 * count3 * count4 * count5
count1 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count2 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count4 * count6
count0 * count4 * count6
count1 * count4 * count6
count0 * count1 * count4 * count6
count2 * count4 * count6
count0 * count2 * count4 * count6
count1 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count3 * count4 * count6
count0 * count3 * count4 * count6
count1 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count2 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count4 * count5 * count6
count0 * count4 * count5 * count6
count1 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count2 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count3 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count4 * count7
count0 * count4 * count7
count1 * count4 * count7
count0 * count1 * count4 * count7
count2 * count4 * count7
count0 * count2 * count4 * count7
count1 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count3 * count4 * count7
count0 * count3 * count4 * count7
count1 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count2 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count4 * count5 * count7
count0 * count4 * count5 * count7
count1 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count2 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count3 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count4 * count6 * count7
count0 * count4 * count6 * count7
count1 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count2 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count3 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count4 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel216 total: 128 terms
# osel217
count0 * count4
count0 * count1 * count4
count0 * count2 * count4
count0 * count1 * count2 * count4
count0 * count3 * count4
count0 * count1 * count3 * count4
count0 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count0 * count4 * count5
count0 * count1 * count4 * count5
count0 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count0 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count4 * count6
count0 * count1 * count4 * count6
count0 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count0 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count4 * count7
count0 * count1 * count4 * count7
count0 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count0 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel217 total: 64 terms
# osel218
count1 * count4
count0 * count1 * count4
count1 * count2 * count4
count0 * count1 * count2 * count4
count1 * count3 * count4
count0 * count1 * count3 * count4
count1 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count1 * count4 * count5
count0 * count1 * count4 * count5
count1 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count1 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count1 * count4 * count6
count0 * count1 * count4 * count6
count1 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count1 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count1 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count4 * count7
count0 * count1 * count4 * count7
count1 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count1 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count1 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel218 total: 64 terms
# osel219
count0 * count1 * count4
count0 * count1 * count2 * count4
count0 * count1 * count3 * count4
count0 * count1 * count2 * count3 * count4
count0 * count1 * count4 * count5
count0 * count1 * count2 * count4 * count5
count0 * count1 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count1 * count4 * count6
count0 * count1 * count2 * count4 * count6
count0 * count1 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count1 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count4 * count7
count0 * count1 * count2 * count4 * count7
count0 * count1 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count1 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel219 total: 32 terms
# osel220
count2 * count4
count0 * count2 * count4
count1 * count2 * count4
count0 * count1 * count2 * count4
count2 * count3 * count4
count0 * count2 * count3 * count4
count1 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count2 * count4 * count5
count0 * count2 * count4 * count5
count1 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count2 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count2 * count4 * count6
count0 * count2 * count4 * count6
count1 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count2 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count2 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count2 * count4 * count7
count0 * count2 * count4 * count7
count1 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count2 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count2 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count2 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel220 total: 64 terms
# osel221
count0 * count2 * count4
count0 * count1 * count2 * count4
count0 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count0 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count0 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count0 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count0 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel221 total: 32 terms
# osel222
count1 * count2 * count4
count0 * count1 * count2 * count4
count1 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count1 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count1 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count4 * count7
count0 * count1 * count2 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel222 total: 32 terms
# osel223
count0 * count1 * count2 * count4
count0 * count1 * count2 * count3 * count4
count0 * count1 * count2 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel223 total: 16 terms
# osel224
count3 * count4
count0 * count3 * count4
count1 * count3 * count4
count0 * count1 * count3 * count4
count2 * count3 * count4
count0 * count2 * count3 * count4
count1 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count3 * count4 * count5
count0 * count3 * count4 * count5
count1 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count2 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count3 * count4 * count6
count0 * count3 * count4 * count6
count1 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count2 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count3 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count3 * count4 * count7
count0 * count3 * count4 * count7
count1 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count2 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count3 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count3 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel224 total: 64 terms
# osel225
count0 * count3 * count4
count0 * count1 * count3 * count4
count0 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count0 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel225 total: 32 terms
# osel226
count1 * count3 * count4
count0 * count1 * count3 * count4
count1 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count1 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count1 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel226 total: 32 terms
# osel227
count0 * count1 * count3 * count4
count0 * count1 * count2 * count3 * count4
count0 * count1 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel227 total: 16 terms
# osel228
count2 * count3 * count4
count0 * count2 * count3 * count4
count1 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count2 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count2 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel228 total: 32 terms
# osel229
count0 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count0 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel229 total: 16 terms
# osel230
count1 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel230 total: 16 terms
# osel231
count0 * count1 * count2 * count3 * count4
count0 * count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel231 total: 8 terms
# osel232
count5
count0 * count5
count1 * count5
count0 * count1 * count5
count2 * count5
count0 * count2 * count5
count1 * count2 * count5
count0 * count1 * count2 * count5
count3 * count5
count0 * count3 * count5
count1 * count3 * count5
count0 * count1 * count3 * count5
count2 * count3 * count5
count0 * count2 * count3 * count5
count1 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count4 * count5
count0 * count4 * count5
count1 * count4 * count5
count0 * count1 * count4 * count5
count2 * count4 * count5
count0 * count2 * count4 * count5
count1 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count3 * count4 * count5
count0 * count3 * count4 * count5
count1 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count2 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count5 * count6
count0 * count5 * count6
count1 * count5 * count6
count0 * count1 * count5 * count6
count2 * count5 * count6
count0 * count2 * count5 * count6
count1 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count3 * count5 * count6
count0 * count3 * count5 * count6
count1 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count2 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count4 * count5 * count6
count0 * count4 * count5 * count6
count1 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count2 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count3 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count5 * count7
count0 * count5 * count7
count1 * count5 * count7
count0 * count1 * count5 * count7
count2 * count5 * count7
count0 * count2 * count5 * count7
count1 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count3 * count5 * count7
count0 * count3 * count5 * count7
count1 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count2 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count4 * count5 * count7
count0 * count4 * count5 * count7
count1 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count2 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count3 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count5 * count6 * count7
count0 * count5 * count6 * count7
count1 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count2 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count3 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count4 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel232 total: 128 terms
# osel233
count0 * count5
count0 * count1 * count5
count0 * count2 * count5
count0 * count1 * count2 * count5
count0 * count3 * count5
count0 * count1 * count3 * count5
count0 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count0 * count4 * count5
count0 * count1 * count4 * count5
count0 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count0 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count5 * count6
count0 * count1 * count5 * count6
count0 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count0 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count5 * count7
count0 * count1 * count5 * count7
count0 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count0 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel233 total: 64 terms
# osel234
count1 * count5
count0 * count1 * count5
count1 * count2 * count5
count0 * count1 * count2 * count5
count1 * count3 * count5
count0 * count1 * count3 * count5
count1 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count1 * count4 * count5
count0 * count1 * count4 * count5
count1 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count1 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count1 * count5 * count6
count0 * count1 * count5 * count6
count1 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count1 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count1 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count5 * count7
count0 * count1 * count5 * count7
count1 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count1 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count1 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel234 total: 64 terms
# osel235
count0 * count1 * count5
count0 * count1 * count2 * count5
count0 * count1 * count3 * count5
count0 * count1 * count2 * count3 * count5
count0 * count1 * count4 * count5
count0 * count1 * count2 * count4 * count5
count0 * count1 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count1 * count5 * count6
count0 * count1 * count2 * count5 * count6
count0 * count1 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count1 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count5 * count7
count0 * count1 * count2 * count5 * count7
count0 * count1 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count1 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel235 total: 32 terms
# osel236
count2 * count5
count0 * count2 * count5
count1 * count2 * count5
count0 * count1 * count2 * count5
count2 * count3 * count5
count0 * count2 * count3 * count5
count1 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count2 * count4 * count5
count0 * count2 * count4 * count5
count1 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count2 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count2 * count5 * count6
count0 * count2 * count5 * count6
count1 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count2 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count2 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count2 * count5 * count7
count0 * count2 * count5 * count7
count1 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count2 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count2 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count2 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel236 total: 64 terms
# osel237
count0 * count2 * count5
count0 * count1 * count2 * count5
count0 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count0 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count0 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count0 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count0 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel237 total: 32 terms
# osel238
count1 * count2 * count5
count0 * count1 * count2 * count5
count1 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count1 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count1 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count5 * count7
count0 * count1 * count2 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel238 total: 32 terms
# osel239
count0 * count1 * count2 * count5
count0 * count1 * count2 * count3 * count5
count0 * count1 * count2 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel239 total: 16 terms
# osel240
count3 * count5
count0 * count3 * count5
count1 * count3 * count5
count0 * count1 * count3 * count5
count2 * count3 * count5
count0 * count2 * count3 * count5
count1 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count3 * count4 * count5
count0 * count3 * count4 * count5
count1 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count2 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count3 * count5 * count6
count0 * count3 * count5 * count6
count1 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count2 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count3 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count3 * count5 * count7
count0 * count3 * count5 * count7
count1 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count2 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count3 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count3 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel240 total: 64 terms
# osel241
count0 * count3 * count5
count0 * count1 * count3 * count5
count0 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count0 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel241 total: 32 terms
# osel242
count1 * count3 * count5
count0 * count1 * count3 * count5
count1 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count1 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count1 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count3 * count5 * count7
count0 * count1 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel242 total: 32 terms
# osel243
count0 * count1 * count3 * count5
count0 * count1 * count2 * count3 * count5
count0 * count1 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count1 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel243 total: 16 terms
# osel244
count2 * count3 * count5
count0 * count2 * count3 * count5
count1 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count2 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count2 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count2 * count3 * count5 * count7
count0 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel244 total: 32 terms
# osel245
count0 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count0 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel245 total: 16 terms
# osel246
count1 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel246 total: 16 terms
# osel247
count0 * count1 * count2 * count3 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel247 total: 8 terms
# osel248
count4 * count5
count0 * count4 * count5
count1 * count4 * count5
count0 * count1 * count4 * count5
count2 * count4 * count5
count0 * count2 * count4 * count5
count1 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count3 * count4 * count5
count0 * count3 * count4 * count5
count1 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count2 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count4 * count5 * count6
count0 * count4 * count5 * count6
count1 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count2 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count3 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count4 * count5 * count7
count0 * count4 * count5 * count7
count1 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count2 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count3 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count4 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel248 total: 64 terms
# osel249
count0 * count4 * count5
count0 * count1 * count4 * count5
count0 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count0 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel249 total: 32 terms
# osel250
count1 * count4 * count5
count0 * count1 * count4 * count5
count1 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count1 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count1 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel250 total: 32 terms
# osel251
count0 * count1 * count4 * count5
count0 * count1 * count2 * count4 * count5
count0 * count1 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count1 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel251 total: 16 terms
# osel252
count2 * count4 * count5
count0 * count2 * count4 * count5
count1 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count2 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count2 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count2 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel252 total: 32 terms
# osel253
count0 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count0 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel253 total: 16 terms
# osel254
count1 * count2 * count4 * count5
count0 * count1 * count2 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel254 total: 16 terms
# osel255
count0 * count1 * count2 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel255 total: 8 terms
# osel256
count3 * count4 * count5
count0 * count3 * count4 * count5
count1 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count2 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count3 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count3 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel256 total: 32 terms
# osel257
count0 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel257 total: 16 terms
# osel258
count1 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel258 total: 16 terms
# osel259
count0 * count1 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count1 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel259 total: 8 terms
# osel260
count2 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel260 total: 16 terms
# osel261
count0 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel261 total: 8 terms
# osel262
count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel262 total: 8 terms
# osel263
count0 * count1 * count2 * count3 * count4 * count5
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel263 total: 4 terms
# osel264
count6
count0 * count6
count1 * count6
count0 * count1 * count6
count2 * count6
count0 * count2 * count6
count1 * count2 * count6
count0 * count1 * count2 * count6
count3 * count6
count0 * count3 * count6
count1 * count3 * count6
count0 * count1 * count3 * count6
count2 * count3 * count6
count0 * count2 * count3 * count6
count1 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count4 * count6
count0 * count4 * count6
count1 * count4 * count6
count0 * count1 * count4 * count6
count2 * count4 * count6
count0 * count2 * count4 * count6
count1 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count3 * count4 * count6
count0 * count3 * count4 * count6
count1 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count2 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count5 * count6
count0 * count5 * count6
count1 * count5 * count6
count0 * count1 * count5 * count6
count2 * count5 * count6
count0 * count2 * count5 * count6
count1 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count3 * count5 * count6
count0 * count3 * count5 * count6
count1 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count2 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count4 * count5 * count6
count0 * count4 * count5 * count6
count1 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count2 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count3 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count6 * count7
count0 * count6 * count7
count1 * count6 * count7
count0 * count1 * count6 * count7
count2 * count6 * count7
count0 * count2 * count6 * count7
count1 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count3 * count6 * count7
count0 * count3 * count6 * count7
count1 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count2 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count4 * count6 * count7
count0 * count4 * count6 * count7
count1 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count2 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count3 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count5 * count6 * count7
count0 * count5 * count6 * count7
count1 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count2 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count3 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count4 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel264 total: 128 terms
# osel265
count0 * count6
count0 * count1 * count6
count0 * count2 * count6
count0 * count1 * count2 * count6
count0 * count3 * count6
count0 * count1 * count3 * count6
count0 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count0 * count4 * count6
count0 * count1 * count4 * count6
count0 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count0 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count5 * count6
count0 * count1 * count5 * count6
count0 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count0 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count6 * count7
count0 * count1 * count6 * count7
count0 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count0 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel265 total: 64 terms
# osel266
count1 * count6
count0 * count1 * count6
count1 * count2 * count6
count0 * count1 * count2 * count6
count1 * count3 * count6
count0 * count1 * count3 * count6
count1 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count1 * count4 * count6
count0 * count1 * count4 * count6
count1 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count1 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count1 * count5 * count6
count0 * count1 * count5 * count6
count1 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count1 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count1 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count6 * count7
count0 * count1 * count6 * count7
count1 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count1 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count1 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel266 total: 64 terms
# osel267
count0 * count1 * count6
count0 * count1 * count2 * count6
count0 * count1 * count3 * count6
count0 * count1 * count2 * count3 * count6
count0 * count1 * count4 * count6
count0 * count1 * count2 * count4 * count6
count0 * count1 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count1 * count5 * count6
count0 * count1 * count2 * count5 * count6
count0 * count1 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count1 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count6 * count7
count0 * count1 * count2 * count6 * count7
count0 * count1 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count1 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel267 total: 32 terms
# osel268
count2 * count6
count0 * count2 * count6
count1 * count2 * count6
count0 * count1 * count2 * count6
count2 * count3 * count6
count0 * count2 * count3 * count6
count1 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count2 * count4 * count6
count0 * count2 * count4 * count6
count1 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count2 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count2 * count5 * count6
count0 * count2 * count5 * count6
count1 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count2 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count2 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count2 * count6 * count7
count0 * count2 * count6 * count7
count1 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count2 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count2 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count2 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel268 total: 64 terms
# osel269
count0 * count2 * count6
count0 * count1 * count2 * count6
count0 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count0 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count0 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count0 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count0 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel269 total: 32 terms
# osel270
count1 * count2 * count6
count0 * count1 * count2 * count6
count1 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count1 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count1 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count6 * count7
count0 * count1 * count2 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel270 total: 32 terms
# osel271
count0 * count1 * count2 * count6
count0 * count1 * count2 * count3 * count6
count0 * count1 * count2 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel271 total: 16 terms
# osel272
count3 * count6
count0 * count3 * count6
count1 * count3 * count6
count0 * count1 * count3 * count6
count2 * count3 * count6
count0 * count2 * count3 * count6
count1 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count3 * count4 * count6
count0 * count3 * count4 * count6
count1 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count2 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count3 * count5 * count6
count0 * count3 * count5 * count6
count1 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count2 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count3 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count3 * count6 * count7
count0 * count3 * count6 * count7
count1 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count2 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count3 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count3 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel272 total: 64 terms
# osel273
count0 * count3 * count6
count0 * count1 * count3 * count6
count0 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count0 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel273 total: 32 terms
# osel274
count1 * count3 * count6
count0 * count1 * count3 * count6
count1 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count1 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count1 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count3 * count6 * count7
count0 * count1 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel274 total: 32 terms
# osel275
count0 * count1 * count3 * count6
count0 * count1 * count2 * count3 * count6
count0 * count1 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count1 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel275 total: 16 terms
# osel276
count2 * count3 * count6
count0 * count2 * count3 * count6
count1 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count2 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count2 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count2 * count3 * count6 * count7
count0 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel276 total: 32 terms
# osel277
count0 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count0 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel277 total: 16 terms
# osel278
count1 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel278 total: 16 terms
# osel279
count0 * count1 * count2 * count3 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel279 total: 8 terms
# osel280
count4 * count6
count0 * count4 * count6
count1 * count4 * count6
count0 * count1 * count4 * count6
count2 * count4 * count6
count0 * count2 * count4 * count6
count1 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count3 * count4 * count6
count0 * count3 * count4 * count6
count1 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count2 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count4 * count5 * count6
count0 * count4 * count5 * count6
count1 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count2 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count3 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count4 * count6 * count7
count0 * count4 * count6 * count7
count1 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count2 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count3 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count4 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel280 total: 64 terms
# osel281
count0 * count4 * count6
count0 * count1 * count4 * count6
count0 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count0 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel281 total: 32 terms
# osel282
count1 * count4 * count6
count0 * count1 * count4 * count6
count1 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count1 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count1 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count4 * count6 * count7
count0 * count1 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel282 total: 32 terms
# osel283
count0 * count1 * count4 * count6
count0 * count1 * count2 * count4 * count6
count0 * count1 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count1 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel283 total: 16 terms
# osel284
count2 * count4 * count6
count0 * count2 * count4 * count6
count1 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count2 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count2 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count2 * count4 * count6 * count7
count0 * count2 * count4 * count6 * count7
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel284 total: 32 terms
# osel285
count0 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count0 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel285 total: 16 terms
# osel286
count1 * count2 * count4 * count6
count0 * count1 * count2 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel286 total: 16 terms
# osel287
count0 * count1 * count2 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel287 total: 8 terms
# osel288
count3 * count4 * count6
count0 * count3 * count4 * count6
count1 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count2 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count3 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count3 * count4 * count6 * count7
count0 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel288 total: 32 terms
# osel289
count0 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel289 total: 16 terms
# osel290
count1 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel290 total: 16 terms
# osel291
count0 * count1 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel291 total: 8 terms
# osel292
count2 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel292 total: 16 terms
# osel293
count0 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel293 total: 8 terms
# osel294
count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel294 total: 8 terms
# osel295
count0 * count1 * count2 * count3 * count4 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel295 total: 4 terms
# osel296
count5 * count6
count0 * count5 * count6
count1 * count5 * count6
count0 * count1 * count5 * count6
count2 * count5 * count6
count0 * count2 * count5 * count6
count1 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count3 * count5 * count6
count0 * count3 * count5 * count6
count1 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count2 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count4 * count5 * count6
count0 * count4 * count5 * count6
count1 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count2 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count3 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count5 * count6 * count7
count0 * count5 * count6 * count7
count1 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count2 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count3 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count4 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel296 total: 64 terms
# osel297
count0 * count5 * count6
count0 * count1 * count5 * count6
count0 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count0 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel297 total: 32 terms
# osel298
count1 * count5 * count6
count0 * count1 * count5 * count6
count1 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count1 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count1 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count5 * count6 * count7
count0 * count1 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel298 total: 32 terms
# osel299
count0 * count1 * count5 * count6
count0 * count1 * count2 * count5 * count6
count0 * count1 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count1 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel299 total: 16 terms
# osel2100
count2 * count5 * count6
count0 * count2 * count5 * count6
count1 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count2 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count2 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count2 * count5 * count6 * count7
count0 * count2 * count5 * count6 * count7
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2100 total: 32 terms
# osel2101
count0 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count0 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2101 total: 16 terms
# osel2102
count1 * count2 * count5 * count6
count0 * count1 * count2 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2102 total: 16 terms
# osel2103
count0 * count1 * count2 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2103 total: 8 terms
# osel2104
count3 * count5 * count6
count0 * count3 * count5 * count6
count1 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count2 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count3 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count3 * count5 * count6 * count7
count0 * count3 * count5 * count6 * count7
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2104 total: 32 terms
# osel2105
count0 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2105 total: 16 terms
# osel2106
count1 * count3 * count5 * count6
count0 * count1 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2106 total: 16 terms
# osel2107
count0 * count1 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2107 total: 8 terms
# osel2108
count2 * count3 * count5 * count6
count0 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2108 total: 16 terms
# osel2109
count0 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2109 total: 8 terms
# osel2110
count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2110 total: 8 terms
# osel2111
count0 * count1 * count2 * count3 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2111 total: 4 terms
# osel2112
count4 * count5 * count6
count0 * count4 * count5 * count6
count1 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count2 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count3 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count4 * count5 * count6 * count7
count0 * count4 * count5 * count6 * count7
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2112 total: 32 terms
# osel2113
count0 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2113 total: 16 terms
# osel2114
count1 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count4 * count5 * count6 * count7
count0 * count1 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2114 total: 16 terms
# osel2115
count0 * count1 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2115 total: 8 terms
# osel2116
count2 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count2 * count4 * count5 * count6 * count7
count0 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2116 total: 16 terms
# osel2117
count0 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2117 total: 8 terms
# osel2118
count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2118 total: 8 terms
# osel2119
count0 * count1 * count2 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2119 total: 4 terms
# osel2120
count3 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count3 * count4 * count5 * count6 * count7
count0 * count3 * count4 * count5 * count6 * count7
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2120 total: 16 terms
# osel2121
count0 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2121 total: 8 terms
# osel2122
count1 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2122 total: 8 terms
# osel2123
count0 * count1 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2123 total: 4 terms
# osel2124
count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count2 * count3 * count4 * count5 * count6 * count7
count0 * count2 * count3 * count4 * count5 * count6 * count7
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2124 total: 8 terms
# osel2125
count0 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2125 total: 4 terms
# osel2126
count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6
count1 * count2 * count3 * count4 * count5 * count6 * count7
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2126 total: 4 terms
# osel2127
count0 * count1 * count2 * count3 * count4 * count5 * count6
count0 * count1 * count2 * count3 * count4 * count5 * count6 * count7
# osel2127 total: 2 terms
# Total terms: 6561
